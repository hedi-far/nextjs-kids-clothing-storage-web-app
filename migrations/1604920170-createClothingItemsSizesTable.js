exports.up = async (sql) => {
  await sql`
  CREATE TABLE IF NOT EXISTS clothing_items_sizes (
		id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY NOT NULL,
		size VARCHAR(20)
	  );
  `;
};

exports.down = async (sql) => {
  await sql`
    DROP TABLE IF EXISTS clothing_items_sizes;
    `;
};
